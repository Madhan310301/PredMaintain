# Environment Variables
# Copy this file to .env and fill in your values

# Database (only needed if using PostgreSQL with Drizzle ORM - not required for in-memory mode)
DATABASE_URL=postgresql://username:password@localhost:5432/predmaintain

# Server
PORT=5000
NODE_ENV=development
